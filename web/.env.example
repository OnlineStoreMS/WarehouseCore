# UserCore 地址（登录跳转，本地开发）
VITE_PORTAL_URL=http://localhost:5174

# 可选：经 PlatformGateway 统一 API（需先启动 Gateway :8088）
# 启用后 IAM 会话校验走 /api/v1/auth/me，不再使用 /iam 代理
# VITE_API_GATEWAY=http://localhost:8088

# 生产构建直连 UserCore 时使用
# VITE_IAM_API_URL=http://your-host:8091/api/v1
